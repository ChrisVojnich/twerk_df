1. Copy your Bearer Token into 'credentials.json'
2. Install necessary dependencies, they are commented at the begining of 'twerk_df_beta.py'
3. Make sure you have the latest Pandas version
4. Open command prompt, access file location:
type "cd " and the folder path, press enter
type "python twerk_df_beta.py" press enter
5. After the script runs after each entry press enter

6. I still have to do error handling
"ValueError: xpath does not return any nodes." means your query has no content, try with a different query